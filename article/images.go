package article

import "github.com/Ethanol48/medium-api/elements"

// downloads the images from a medium article
// it works by changing the `Src` value of the image to match
// the file downloaded obtained by the `Src` and the `Id` from
// the Image struct
func DownloadImages(art elements.Article) {
	// iterate in articles components to search the
}
